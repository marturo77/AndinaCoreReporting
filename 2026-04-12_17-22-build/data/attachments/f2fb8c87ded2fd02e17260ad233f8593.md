# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: cotizacion\carga_masiva.feature.spec.js >> R05 Carga masiva de cotizacion >> R05-C01 carga masiva de cotizacion y consulta de resultados
- Location: .features-gen\cotizacion\carga_masiva.feature.spec.js:11:7

# Error details

```
Error: page.waitForTimeout: Target page, context or browser has been closed
```

# Test source

```ts
  53  |   const otpModalTitle = page
  54  |     .locator("text=Digite el código OTP enviado a su correo electrónico")
  55  |     .first();
  56  |   const homeReady = page
  57  |     .locator('input[aria-label="Buscar"], [aria-label*="Avatar de"]')
  58  |     .first();
  59  | 
  60  |   const waitForAuthSurface = async (timeout: number) =>
  61  |     Promise.race([
  62  |       otpInputs.first().waitFor({ state: "visible", timeout }).then(() => "otp" as const).catch(() => null),
  63  |       homeReady.waitFor({ state: "visible", timeout }).then(() => "home" as const).catch(() => null),
  64  |       page.waitForURL(/\/inicio(?:\/)?$/, { timeout }).then(() => "home" as const).catch(() => null),
  65  |     ]);
  66  | 
  67  |   let authSurface = await waitForAuthSurface(20_000);
  68  | 
  69  |   if (!authSurface && (await loginButton.isVisible().catch(() => false))) {
  70  |     console.info("[LOGIN] Reintentando click en Ingresar");
  71  |     await loginButton.click({ timeout: 20_000, force: true }).catch(() => {});
  72  |     authSurface = await waitForAuthSurface(20_000);
  73  |   }
  74  | 
  75  |   const otpRequired = authSurface === "otp";
  76  | 
  77  |   if (!otpRequired) {
  78  |     await page.waitForLoadState("networkidle", { timeout: 30_000 }).catch(() => {});
  79  |     await homeReady.waitFor({ state: "visible", timeout: 30_000 });
  80  |     await page.waitForTimeout(3_000);
  81  |     await takeScreenshot(page, "Sesion iniciada");
  82  |     return;
  83  |   }
  84  | 
  85  |   const validarButton = page.locator("//button[contains(., 'Validar')]");
  86  |   const reenviarButton = page.locator("//button[contains(., 'Reenviar') and not(@disabled)]");
  87  |   await validarButton.waitFor({ state: "visible", timeout: 20_000 });
  88  | 
  89  |   const llenarOtp = async (otpResponse: Awaited<ReturnType<typeof waitForOtpResponse>>) => {
  90  |     if (!otpResponse) {
  91  |       throw new Error(`No se pudo capturar la respuesta OTP para ${email}.`);
  92  |     }
  93  | 
  94  |     const otpPayload = (await otpResponse.json()) as { otp?: string | number };
  95  |     const otpCode = String(otpPayload?.otp ?? "").trim();
  96  | 
  97  |     if (!otpCode) {
  98  |       throw new Error(`No se pudo capturar OTP para ${email} desde Network.`);
  99  |     }
  100 | 
  101 |     const inputCount = await otpInputs.count();
  102 |     for (let i = 0; i < inputCount; i += 1) {
  103 |       await otpInputs.nth(i).fill("");
  104 |     }
  105 | 
  106 |     for (let i = 0; i < otpCode.length && i < inputCount; i += 1) {
  107 |       await otpInputs.nth(i).fill(otpCode[i]);
  108 |     }
  109 |   };
  110 | 
  111 |   let autenticado = false;
  112 |   for (let intento = 1; intento <= 4 && !autenticado; intento += 1) {
  113 |     const otpResponse =
  114 |       intento === 1
  115 |         ? await otpResponsePromise
  116 |         : await (async () => {
  117 |             const resendOtpResponsePromise = waitForOtpResponse();
  118 |             await reenviarButton.click({ timeout: 20_000 }).catch(() => {});
  119 |             return resendOtpResponsePromise;
  120 |           })();
  121 | 
  122 |     await llenarOtp(otpResponse);
  123 |     await page
  124 |       .locator("//button[contains(., 'Validar') and not(@disabled)]")
  125 |       .waitFor({ state: "visible", timeout: 20_000 });
  126 |     await validarButton.click({ timeout: 20_000 });
  127 | 
  128 |     autenticado = await Promise.race([
  129 |       otpModalTitle.waitFor({ state: "hidden", timeout: 30_000 }).then(() => true).catch(() => false),
  130 |       homeReady.waitFor({ state: "visible", timeout: 30_000 }).then(() => true).catch(() => false),
  131 |       page.waitForURL(/\/inicio(?:\/)?$/, { timeout: 30_000 }).then(() => true).catch(() => false),
  132 |     ]);
  133 | 
  134 |     if (!autenticado) {
  135 |       console.info(`[LOGIN] Reintento de validacion OTP ${intento}`);
  136 |       if (page.isClosed()) {
  137 |         break;
  138 |       }
  139 |       await page.waitForTimeout(2_000);
  140 |     }
  141 |   }
  142 | 
  143 |   if (!autenticado) {
  144 |     autenticado = await homeReady.isVisible().catch(() => false);
  145 |   }
  146 | 
  147 |   if (!autenticado) {
  148 |     throw new Error("No fue posible completar la validacion OTP.");
  149 |   }
  150 | 
  151 |   await page.waitForLoadState("networkidle", { timeout: 30_000 }).catch(() => {});
  152 |   await homeReady.waitFor({ state: "visible", timeout: 30_000 });
> 153 |   await page.waitForTimeout(3_000);
      |              ^ Error: page.waitForTimeout: Target page, context or browser has been closed
  154 |   await takeScreenshot(page, "Sesion iniciada");
  155 | });
  156 | 
  157 | Then(/^carga la pagina$/, async ({ page }) => {
  158 |   await page.waitForURL(/.+/, { timeout: 20_000 });
  159 |   await takeScreenshot(page, "Pantalla de bienvenida");
  160 | });
  161 | 
```