# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: cotizacion\carga_manual.feature.spec.js >> R03 Creacion manual de cotizacion >> R03-C02 creacion manual de cotizacion desde archivo
- Location: .features-gen\cotizacion\carga_manual.feature.spec.js:12:7

# Error details

```
Error: La pagina se cerro antes de validar el valor del combo 'MASCULINO': page.waitForTimeout: Test ended.
```

# Test source

```ts
  97  |       if (valorEsperado && await this.tryFillByTyping(combo, valorEsperado)) {
  98  |         await this.assertComboValue(combo, valorEsperado);
  99  |         return;
  100 |       }
  101 | 
  102 |       throw error;
  103 |     }
  104 | 
  105 |     const opcionesVisibles = await this.optionTexts();    
  106 | 
  107 |     if (optionIndex !== undefined && optionIndex !== null) {
  108 |       const opciones = this.options();
  109 |       const opcionPorIndice = opciones.nth(optionIndex);
  110 |       await opcionPorIndice.waitFor({ state: 'visible', timeout: 5000 });
  111 |       await opcionPorIndice.click({ timeout: 5000 });
  112 |       await this.assertComboValue(combo, valorEsperado);
  113 |       return;
  114 |     }
  115 | 
  116 |     if (valorEsperado) {
  117 |       const exactOption = await this.optionExact(valorEsperado);
  118 |       if (exactOption) {
  119 |         await exactOption.click({ timeout: 5000 });
  120 |         await this.assertComboValue(combo, valorEsperado);
  121 |         return;
  122 |       }
  123 | 
  124 |       const matchingOption = await this.optionBest(valorEsperado);
  125 |       if (matchingOption) {
  126 |         await matchingOption.click({ timeout: 5000 });
  127 |         await this.assertComboValue(combo, valorEsperado);
  128 |         return;
  129 |       }
  130 | 
  131 |       if (await this.tryFillByTyping(combo, valorEsperado)) {
  132 |         await this.assertComboValue(combo, valorEsperado);
  133 |         return;
  134 |       }
  135 | 
  136 |       throw new Error(
  137 |         `No se encontro una opcion visible para '${valorEsperado}'. Opciones: ${opcionesVisibles.join(' | ')}`
  138 |       );
  139 |     }
  140 | 
  141 |     const fallbackOption = this.firstOption();
  142 |     await fallbackOption.waitFor({ state: 'visible', timeout: 5000 });
  143 |     await fallbackOption.click({ timeout: 10000 });
  144 |   }
  145 | 
  146 |   private async sectionRoot(section: FormSection): Promise<Locator> {
  147 |     const heading = this.page.locator('h3.title-expansion').filter({
  148 |       hasText: new RegExp(this.rx(section), 'i'),
  149 |     }).first();
  150 |     const sectionRoot = heading.locator(
  151 |       "xpath=ancestor::*[self::div or self::section][.//h3[contains(@class,'title-expansion')] and (.//input or .//textarea or .//*[@role='combobox'])][1]"
  152 |     );
  153 |     await sectionRoot.waitFor({ state: 'visible', timeout: 5000 });
  154 |     return sectionRoot;
  155 |   }
  156 | 
  157 |   private async openCombo(combo: Locator, comboNombre: string): Promise<void> {
  158 |     let lastError: unknown;
  159 | 
  160 |     try {
  161 |       await combo.click({ timeout: 5000 }).catch(async () => {
  162 |         await combo.click({ timeout: 5000, force: true });
  163 |       });
  164 |       await this.waitOptions(comboNombre);
  165 |       return;
  166 |     } catch (error) {
  167 |       lastError = error;
  168 |     }
  169 | 
  170 |     try {
  171 |       await combo.focus().catch(() => {});
  172 |       await this.page.keyboard.press('Space').catch(() => {});
  173 |       await this.page.keyboard.press('ArrowDown').catch(() => {});
  174 |       await this.waitOptions(comboNombre);
  175 |       return;
  176 |     } catch (error) {
  177 |       lastError = error;
  178 |     }
  179 | 
  180 |     throw new Error(
  181 |       `No fue posible desplegar opciones para el combo '${comboNombre}': ${this.describeError(lastError)}`
  182 |     );
  183 |   }
  184 | 
  185 |   private async hasVisibleOptions(): Promise<boolean> {
  186 |     return (await this.options().count().catch(() => 0)) > 0;
  187 |   }
  188 | 
  189 |   private async assertComboValue(combo: Locator, valorEsperado: string): Promise<void> {
  190 |     if (!valorEsperado) {
  191 |       return;
  192 |     }
  193 | 
  194 |     try {
  195 |       await this.page.waitForTimeout(COMBO_POLL_INTERVAL_MS);
  196 |     } catch (error) {
> 197 |       throw new Error(
      |             ^ Error: La pagina se cerro antes de validar el valor del combo 'MASCULINO': page.waitForTimeout: Test ended.
  198 |         `La pagina se cerro antes de validar el valor del combo '${valorEsperado}': ${this.describeError(error)}`
  199 |       );
  200 |     }
  201 |     const comboText = await this.readComboValue(combo);
  202 |     if (!this.hasExpectedValue(comboText, valorEsperado)) {
  203 |       throw new Error(`El combo no quedo con el valor esperado '${valorEsperado}'. Valor actual: '${comboText}'.`);
  204 |     }
  205 |   }
  206 | 
  207 |   private async pick(locator: Locator): Promise<Locator> {
  208 |     const locatorText = String(locator);
  209 |     const totalCandidatos = await locator.count().catch(() => 0);
  210 |     if (!totalCandidatos) {
  211 |       throw new Error(`No se encontro ningun combo asociado al locator '${locatorText}'.`);
  212 |     }
  213 | 
  214 |     const limiteCandidatos = Math.min(totalCandidatos, 25);
  215 | 
  216 |     for (let index = 0; index < limiteCandidatos; index += 1) {
  217 |       const candidato = locator.nth(index);
  218 | 
  219 |       try {
  220 |         await candidato.waitFor({ state: 'visible', timeout: 1000 });
  221 |         await candidato.scrollIntoViewIfNeeded();
  222 |       } catch {
  223 |         continue;
  224 |       }
  225 | 
  226 |       const contenedorComboLocator = candidato.locator(
  227 |         "xpath=ancestor-or-self::*[contains(@class,'q-select') or contains(@class,'q-field')][1]"
  228 |       ).first();
  229 | 
  230 |       const posiblesTargets = [
  231 |         contenedorComboLocator.locator("[role='combobox']:not([disabled])").first(),
  232 |         contenedorComboLocator.locator("input[role='combobox']:not([disabled])").first(),
  233 |         contenedorComboLocator.locator('.q-field__control').first(),
  234 |         candidato,
  235 |         contenedorComboLocator,
  236 |       ];
  237 | 
  238 |       for (const targetLocator of posiblesTargets) {
  239 |         try {
  240 |           if (!(await targetLocator.isVisible().catch(() => false))) {
  241 |             continue;
  242 |           }
  243 | 
  244 |           if (!(await this.isInteractableComboTarget(targetLocator))) {
  245 |             continue;
  246 |           }
  247 | 
  248 |           await targetLocator.scrollIntoViewIfNeeded().catch(() => {});
  249 | 
  250 |           const boundingBox = await targetLocator.boundingBox().catch(() => null);
  251 |           if (boundingBox && boundingBox.width > 0 && boundingBox.height > 0) {
  252 |             return targetLocator;
  253 |           }
  254 |         } catch {
  255 |           // Intenta con el siguiente target interactuable.
  256 |         }
  257 |       }
  258 |     }
  259 | 
  260 |     throw new Error(`No se encontro un combo visible para el locator '${locatorText}'.`);
  261 |   }
  262 | 
  263 |   private async byColumnName(
  264 |     searchRoot: Locator | Page,
  265 |     nombreElemento: string
  266 |   ): Promise<Locator | undefined> {
  267 |     const normalizedName = this.normLabel(nombreElemento);
  268 |     const columns = searchRoot.locator(
  269 |       "form.q-form.q-gutter-md div[class*='col-xs-12'], " +
  270 |       "div[class*='col-xs-12'][class*='col-sm-6'], " +
  271 |       "div[class*='col-xs-12'][class*='col-md-3'], " +
  272 |       "div[class*='col-xs-12'][class*='col-md-4'], " +
  273 |       "div[class*='col-xs-12'][class*='col-lg-3'], " +
  274 |       "div[class*='col-xs-12'][class*='col-lg-4']"
  275 |     );
  276 | 
  277 |     const totalColumns = await columns.count();
  278 |     for (let index = 0; index < totalColumns; index += 1) {
  279 |       const column = columns.nth(index);
  280 |       const title = column.locator('.title').first();
  281 |       const columnText = this.normLabel((await title.textContent().catch(() => '')) ?? '');
  282 |       const matchesTitle = !!columnText && this.labelsMatch(columnText, normalizedName);
  283 |       if (!matchesTitle) {
  284 |         continue;
  285 |       }
  286 | 
  287 |       await column.scrollIntoViewIfNeeded().catch(() => {});
  288 | 
  289 |       const comboCandidates = [
  290 |         column.locator(".q-select, .q-field").first(),
  291 |         column.locator("[role='combobox']").first(),
  292 |         column.locator(".q-select .q-field__control, .q-field .q-field__control").first(),
  293 |         column.locator("input[role='combobox']").first(),
  294 |       ];
  295 | 
  296 |       for (const candidate of comboCandidates) {
  297 |         try {
```