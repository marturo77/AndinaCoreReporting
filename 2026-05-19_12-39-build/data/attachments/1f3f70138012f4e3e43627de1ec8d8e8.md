# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: cotizacion\carga_manual.feature.spec.js >> R03 Creacion manual de cotizacion >> R03-C02 creacion manual de cotizacion desde archivo
- Location: .features-gen\cotizacion\carga_manual.feature.spec.js:12:7

# Error details

```
TimeoutError: locator.waitFor: Timeout 20000ms exceeded.
Call log:
  - waiting for getByRole('heading', { name: /Crear cotizaci.n/i }) to be visible

```

# Test source

```ts
  426 |   }
  427 | 
  428 |   async navegarYSeleccionarFecha(fechaObjetivo: Date | string, maxIntentos: number = 48): Promise<void> {
  429 |     console.log(`Navegando en calendario para seleccionar la fecha: ${fechaObjetivo}...`);
  430 | 
  431 |     const fecha = this.parseFechaObjetivo(fechaObjetivo);
  432 | 
  433 |     const mesesANumero: Record<string, number> = {
  434 |       enero: 1,
  435 |       febrero: 2,
  436 |       marzo: 3,
  437 |       abril: 4,
  438 |       mayo: 5,
  439 |       junio: 6,
  440 |       julio: 7,
  441 |       agosto: 8,
  442 |       septiembre: 9,
  443 |       octubre: 10,
  444 |       noviembre: 11,
  445 |       diciembre: 12,
  446 |     };
  447 | 
  448 |     for (let intentos = 0; intentos < maxIntentos; intentos++) {
  449 |       if (this.page.isClosed()) {
  450 |         throw new Error('La pagina se cerro mientras se navegaba el calendario.');
  451 |       }
  452 | 
  453 |       const yearText =
  454 |         (await this.page.locator(SELECTORS.yearNavigation).textContent({ timeout: 10000 }))?.trim() || '';
  455 |       const monthText =
  456 |         (await this.page.locator(SELECTORS.monthNavigation).textContent({ timeout: 10000 }))?.trim() || '';
  457 | 
  458 |       const parseYear = (...texts: string[]): number | null => {
  459 |         for (const txt of texts) {
  460 |           const cleaned = txt.trim();
  461 |           if (!cleaned) continue;
  462 |           if (/^\d+$/.test(cleaned)) return parseInt(cleaned, 10);
  463 |           const match = cleaned.match(/\d{4}/);
  464 |           if (match) return parseInt(match[0], 10);
  465 |         }
  466 |         return null;
  467 |       };
  468 | 
  469 |       const parseMonth = (...texts: string[]): number | null => {
  470 |         for (const txt of texts) {
  471 |           const monthValue = mesesANumero[txt.trim().toLowerCase()];
  472 |           if (monthValue) return monthValue;
  473 |         }
  474 |         return null;
  475 |       };
  476 | 
  477 |       const currentYear = parseYear(yearText, monthText) ?? fecha.getFullYear();
  478 |       const currentMonth = parseMonth(monthText, yearText);
  479 | 
  480 |       if (currentMonth === null) {
  481 |         throw new Error(
  482 |           `No se pudo interpretar a̱o/mes actuales: a̱o='${yearText}', mes='${monthText}'`
  483 |         );
  484 |       }
  485 | 
  486 |       if (currentYear === fecha.getFullYear() && currentMonth === fecha.getMonth() + 1) {
  487 |         await this.page.waitForSelector(SELECTORS.calendarDays, { timeout: 10000 });
  488 |         const dayLocator = this.page.locator(
  489 |           `${SELECTORS.calendarDays}//button` +
  490 |             "[not(contains(@class,'q-date__calendar-item--out')) and not(@aria-disabled='true')]" +
  491 |             `[.//span[text()='${fecha.getDate()}']]`
  492 |         );
  493 |         await dayLocator.first().click({ timeout: 10000 });
  494 |         return;
  495 |       }
  496 | 
  497 |       if (currentYear !== fecha.getFullYear()) {
  498 |         const yearButton =
  499 |           currentYear > fecha.getFullYear()
  500 |             ? this.page.locator(SELECTORS.prevYearButton)
  501 |             : this.page.locator(SELECTORS.nextYearButton);
  502 |         await yearButton.click({ timeout: 10_000 });
  503 |         await this.page.waitForTimeout(150);
  504 |         continue;
  505 |       }
  506 | 
  507 |       if (currentMonth > fecha.getMonth() + 1) {
  508 |         await this.page.locator(SELECTORS.prevMonthButton).click({ timeout: 10_000 });
  509 |       } else {
  510 |         await this.page.locator(SELECTORS.nextMonthButton).click({ timeout: 10_000 });
  511 |       }
  512 | 
  513 |       await this.page.waitForTimeout(150);
  514 |     }
  515 | 
  516 |     throw new Error(
  517 |       `No se pudo navegar hasta la fecha objetivo '${this.formatearFecha(fecha)}' en el calendario.`
  518 |     );
  519 |   }
  520 | 
  521 |   async openCotizacion(): Promise<void> {
  522 |     console.log('Abriendo la pagina de cotizacion...');
  523 |     await this.page.goto(this.cotizacionUrl);
  524 |     await this.page.waitForLoadState('domcontentloaded');
  525 |     try {
> 526 |       await this.page.getByRole('heading', { name: /Crear cotizaci.n/i }).waitFor({
      |                                                                           ^ TimeoutError: locator.waitFor: Timeout 20000ms exceeded.
  527 |         state: 'visible',
  528 |         timeout: DEFAULT_TIMEOUT,
  529 |       });
  530 |       await this.page.evaluate(`document.body.style.zoom='${BODY_ZOOM}'`);
  531 |     } catch (error) {
  532 |       console.log(`URL actual al abrir cotizacion: ${this.page.url()}`);
  533 |       console.log(
  534 |         'Texto visible actual:',
  535 |         await this.page.locator('body').innerText().catch(() => '[sin body]')
  536 |       );
  537 |       throw error;
  538 |     }
  539 |     await this.esperarSpinner();
  540 |     console.log('PAGINA CARGADA: COTIZACION');
  541 |   }
  542 | 
  543 |   async uploadContratacionFile(
  544 |     _context: any,
  545 |     nombreArchivo: string = 'contratacion.xlsx'
  546 |   ): Promise<void> {
  547 |     console.log('Subiendo archivo de contratacion...');
  548 |     const excel = this.prepararArchivoContratacion(`cotizacion/${nombreArchivo}`);
  549 |     if (!fs.existsSync(excel)) {
  550 |       throw new Error(`No existe el archivo Excel: ${excel}`);
  551 |     }
  552 | 
  553 |     const maxIntentos = 3;
  554 |     for (let intento = 1; intento <= maxIntentos; intento += 1) {
  555 |       await this.abrirFlujoCargue(/Cargue contrataci.n/i);
  556 |       await this.subirArchivoYDescargarResultados(excel);
  557 | 
  558 |       const mensajes = this.obtenerMensajesResultadoContratacion(this.lastDownloadPath);
  559 |       const pendienteCotizada = mensajes.some((mensaje) =>
  560 |         new RegExp(`no est.*estado '${EstadoCotizacion.COTIZADA}'`, 'i').test(mensaje)
  561 |       );
  562 | 
  563 |       if (!pendienteCotizada) {
  564 |         return;
  565 |       }
  566 | 
  567 |       if (intento === maxIntentos) {
  568 |         console.log(`La cotizacion sigue sin estado ${EstadoCotizacion.COTIZADA} despues de los reintentos permitidos.`);
  569 |         return;
  570 |       }
  571 | 
  572 |       const esperaSegundos = 15 * intento;
  573 |       console.log(
  574 |         `La cotizacion aun no aparece como ${EstadoCotizacion.COTIZADA} para contratacion. Reintentando en ${esperaSegundos}s (intento ${intento + 1}/${maxIntentos}).`
  575 |       );
  576 |       await this.page.waitForTimeout(esperaSegundos * 1000);
  577 |     }
  578 |   }
  579 | 
  580 |   async uploadCotizacionFile(_context: any, nombreArchivo: string): Promise<void> {
  581 |     console.log('Subiendo archivo de cotizacion...');
  582 |     await this.page.evaluate(`document.body.style.zoom='${BODY_ZOOM}'`);
  583 | 
  584 |     const preparedRuntime = await prepararRuntimesMasivos(`cotizacion/${nombreArchivo}`, 'cotizacion/contratacion.xlsx');
  585 |     const excel = getRutaRuntimeCotizacionMasiva();
  586 |     const cotizaciones = asignarOrigenRegistroCotizaciones(
  587 |       preparedRuntime.cotizacion.cotizaciones,
  588 |       ORIGEN_REGISTRO_COTIZACION_MASIVA
  589 |     );
  590 |     const cotizacionesAutomaticasExistentes = filtrarCotizacionesPorOrigenRegistro(
  591 |       readCotizaciones(),
  592 |       ORIGEN_REGISTRO_COTIZACION_MASIVA
  593 |     );
  594 |     const cotizacionesCombinadas = mergeCotizaciones(cotizacionesAutomaticasExistentes, cotizaciones);
  595 |     saveCotizaciones(cotizacionesCombinadas);
  596 |     console.log(`Runtime de cotizacion disponible en: ${preparedRuntime.cotizacion.artifactPath}`);
  597 |     await this.abrirFlujoCargue(/Cargue cotizaci.n/i, /Cargue de cotizaci.n/i);
  598 |     await this.subirArchivoYDescargarResultados(excel);
  599 |   }
  600 | 
  601 |   private obtenerMensajesResultadoContratacion(resultadosPath?: string): string[] {
  602 |     const resolvedPath = resultadosPath ?? this.lastDownloadPath;
  603 |     if (!resolvedPath || !fs.existsSync(resolvedPath)) {
  604 |       return [];
  605 |     }
  606 | 
  607 |     const { worksheet } = leerPrimeraHoja(resolvedPath);
  608 |     const rows = leerFilasDeHoja<ExcelRow>(worksheet);
  609 |     return rows
  610 |       .map((row) => String(row['Mensajes'] ?? '').trim())
  611 |       .filter(Boolean);
  612 |   }
  613 | 
  614 |   async validarResultadosCargueContratacion(
  615 |     resultadosPath: string
  616 |   ): Promise<ResultadoValidacionContratacion> {
  617 |     if (!fs.existsSync(resultadosPath)) {
  618 |       throw new Error(`No existe el archivo de resultados de contratacion: ${resultadosPath}`);
  619 |     }
  620 | 
  621 |     const { worksheet } = leerPrimeraHoja(resultadosPath);
  622 |     const rows = leerFilasDeHoja<ExcelRow>(worksheet);
  623 | 
  624 |     if (rows.length === 0) {
  625 |       console.log('Archivo de resultados sin datos');
  626 |       return {
```